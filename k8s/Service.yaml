apiVersion: v1
kind: Service
metadata:
  name: unity-server

spec:
  type: LoadBalancer

  selector:
    app: unity-server

  ports:
    - name: game
      port: 7777
      targetPort: 7777
      protocol: UDP